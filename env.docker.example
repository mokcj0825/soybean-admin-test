# ===========================================
# Docker Compose 环境变量配置模板
# ===========================================
# 使用说明：
# 1. 复制此文件为 .env: cp env.docker.example .env
# 2. 根据需要修改配置值
# 3. 运行 docker-compose up -d

# ===========================================
# Docker Compose 项目配置
# ===========================================
# 项目名称（设置后无需使用 -p 参数）
COMPOSE_PROJECT_NAME=soybean-admin-nest

# ===========================================
# 数据库配置 (PostgreSQL)
# ===========================================
# 数据库用户名
POSTGRES_USER=soybean
# 数据库密码
POSTGRES_PASSWORD=soybean@123.
# 数据库名称
POSTGRES_DB=soybean-admin-nest-backend
# 数据库端口（宿主机映射端口）
DATABASE_PORT=25432
# 数据库内部端口（容器内部，一般不需要修改）
DATABASE_INTERNAL_PORT=5432

# ===========================================
# Redis 配置
# ===========================================
# Redis 密码
REDIS_PASSWORD=123456
# Redis 端口（宿主机映射端口）
REDIS_PORT=26379
# Redis 内部端口（容器内部，一般不需要修改）
REDIS_INTERNAL_PORT=6379
# Redis 数据库编号
REDIS_DB=1

# ===========================================
# 后端配置 (Backend)
# ===========================================
# 后端服务端口
BACKEND_PORT=9528
# Node 环境
NODE_ENV=production
# Swagger 文档是否启用
DOC_SWAGGER_ENABLE=true
# Swagger 文档路径
DOC_SWAGGER_PATH=api-docs
# Casbin 模型文件（一般不需要修改）
CASBIN_MODEL=model.conf

# ===========================================
# JWT 配置
# ===========================================
# JWT 密钥
JWT_SECRET=JWT_SECRET-soybean-admin-nest@123456!@#.
# JWT 过期时间（秒）
JWT_EXPIRE_IN=3600
# 刷新令牌密钥
REFRESH_TOKEN_SECRET=REFRESH_TOKEN_EXPIRE_IN-soybean-admin-nest@123456!@#.
# 刷新令牌过期时间（秒）
REFRESH_TOKEN_EXPIRE_IN=7200

# ===========================================
# 前端配置 (Frontend)
# ===========================================
# 前端服务端口
FRONTEND_PORT=9527

# ===========================================
# PgBouncer 配置
# ===========================================
# PgBouncer 端口（宿主机映射端口）
PGBOUNCER_PORT=6432

# ===========================================
# 时区配置
# ===========================================
TZ=Asia/Shanghai

